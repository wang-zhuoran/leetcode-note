# 【力扣刷题笔记】数组与双指针：从入门到踩坑实录

**摘要**：本文记录了 LeetCode 数组类题目中常见的双指针解法。重点复盘了在 `vector.size()` 无符号运算、数组下标越界、以及栈模拟中的常见错误与修正方案。

---

## 1. 移除元素 (27. Remove Element)

**题目链接**：[LeetCode 27](https://leetcode.cn/problems/remove-element/)
**核心思想**：快慢指针法。快指针寻找“新元素”，慢指针指示“新位置”。

### ❌ 踩坑记录：循环条件与无符号整数溢出

**错误代码片段**：
```cpp
// 错误 1：循环条件逻辑死锁
while(fast > slow && ...) // 初始 0>0 为 false，循环进不去

// 错误 2：无符号整数溢出陷阱
while(fast <= nums.size() - 1) 
// 当 nums 为空时，nums.size() 是 unsigned 0。
// 0 - 1 在无符号运算中会变成巨大的正整数，导致越界访问崩溃。
```

### ✅ 正确解法
```cpp
class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int slow = 0;
        // 使用 fast < nums.size() 避免无符号溢出问题
        for (int fast = 0; fast < nums.size(); fast++) {
            // fast 负责寻找不需要移除的元素
            if (nums[fast] != val) {
                nums[slow] = nums[fast]; // 赋值给 slow
                slow++; // slow 前进
            }
        }
        return slow; // slow 指向新数组的尾部，刚好是新长度
    }
};
```

### 💡 学到了什么 & 注意点
1.  **双指针定义**：`fast` 指针探路，`slow` 指针占位。
2.  **C++ 陷阱**：`vector.size()` 返回的是 `size_t` (unsigned)。**永远不要写 `i <= nums.size() - 1`**，因为空数组时会发生溢出（Underflow）。一定要写成 **`i < nums.size()`**。
3.  **返回值**：`slow` 代表处理后的元素个数。

---

## 2. 删除有序数组中的重复项 (26. Remove Duplicates from Sorted Array)

**题目链接**：[LeetCode 26](https://leetcode.cn/problems/remove-duplicates-from-sorted-array/)
**核心思想**：快慢指针。因为数组有序，重复元素一定相邻。

### ❓ 疑惑点：为什么返回 `slow + 1`？

**代码核心**：
```cpp
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if(nums.empty()) return 0;
        int slow = 0, fast = 0;
        while(fast < nums.size()) {
            if(nums[slow] != nums[fast]) {
                slow++; // 先移动
                nums[slow] = nums[fast]; // 再赋值
            }
            fast++;
        }
        return slow + 1; // 这里为什么要 +1？
    }
};
```

### 💡 学到了什么 & 注意点
1.  **下标 vs 长度**：
    *   `slow` 是去重后最后一个有效元素的**下标 (Index)**。
    *   题目要求返回新数组的**长度 (Length)**。
    *   **长度 = 最大下标 + 1**。
2.  **逻辑顺序**：先判断不同，`slow` 先前移一位，再接收 `fast` 的值（保留第一个，覆盖重复的）。

---

## 3. 有序数组的平方 (977. Squares of a Sorted Array)

**题目链接**：[LeetCode 977](https://leetcode.cn/problems/squares-of-a-sorted-array/)
**核心思想**：双指针从两头向中间靠拢（最大的数一定在两端），新建数组从后往前填。

### ❌ 踩坑记录：数组大小分配与越界

**错误代码片段**：
```cpp
int len = nums.size() - 1; 
vector<int> res(len); // 错误：大小只开了 n-1
...
res[len] = ...; // 错误：访问 res[n-1] 越界，因为 size 只有 n-1
```

### ✅ 正确解法
```cpp
class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int n = nums.size();
        vector<int> res(n); // 1. 大小要开够 n
        
        int k = n - 1; // 2. 写入位置从最后一个下标开始
        int l = 0, r = n - 1;
        
        while(l <= r) {
            long long leftSquare = (long long)nums[l] * nums[l];
            long long rightSquare = (long long)nums[r] * nums[r];
            
            if(leftSquare > rightSquare) {
                res[k] = leftSquare;
                l++;
            } else {
                res[k] = rightSquare;
                r--;
            }
            k--; // 3. 填完一个，位置前移
        }
        return res;
    }
};
```

### 💡 学到了什么 & 注意点
1.  **Size vs Index**：
    *   定义数组用 **Size** (`n`)。
    *   访问数组最后一个元素用 **Index** (`n - 1`)。
2.  **不要混用变量**：建议单独定义一个 `k` 指针来控制结果数组的写入位置，不要复用 `len` 变量，容易逻辑混乱。

---

## 4. 比较含退格的字符串 (844. Backspace String Compare)

**题目链接**：[LeetCode 844](https://leetcode.cn/problems/backspace-string-compare/)
**核心思想**：栈（Stack）思想。遇到普通字符入栈，遇到 `#` 出栈。

### ❓ 技巧：C++ 中如何优雅地使用栈？

**解法一：String 模拟栈（推荐）**
在 C++ 中，`std::string` 本身就具备栈的特性，不需要额外引入 `<stack>` 库。

```cpp
class Solution {
public:
    // 辅助函数：将含退格的字符串转换为最终形态
    string build(string str) {
        string ret; // 把 string 当栈用
        for (char c : str) {
            if (c != '#') {
                ret.push_back(c); // 入栈
            } else if (!ret.empty()) {
                ret.pop_back();   // 出栈
            }
        }
        return ret;
    }

    bool backspaceCompare(string s, string t) {
        return build(s) == build(t); // 直接比较处理后的字符串
    }
};
```

**解法二：双指针（O(1) 空间进阶）**
*   **思路**：从后往前遍历。遇到 `#` 记录跳过次数，跳过下一个非 `#` 字符。

### 💡 学到了什么 & 注意点
1.  **String API**：
    *   `push_back(c)`: 尾部添加（入栈）。
    *   `pop_back()`: 尾部删除（出栈）。
    *   `back()`: 获取尾部元素（栈顶）。
    *   `empty()`: 判空。
2.  **面试技巧**：先用 String 模拟栈秒杀题目，如果面试官追问空间优化，再写从后往前的双指针。

---

## 总结：刷题心得

1.  **不要怕多定义变量**：下标 `index` 和长度 `size` 最好分开，混用容易出错。
2.  **无符号整数的坑**：写循环条件时，尽量避免 `nums.size() - 1`，改用 `i < nums.size()`。
3.  **双指针的本质**：
    *   **同向双指针**（快慢指针）：用于原地修改数组（去重、移除）。
    *   **对向双指针**（左右指针）：用于基于值的查找或排序（有序数组平方、两数之和）。
    *   **逆向双指针**：用于处理具有“抵消”性质的序列（退格字符串）。
